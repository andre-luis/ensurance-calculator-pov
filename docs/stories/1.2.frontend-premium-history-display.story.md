# Story 1.2: Frontend: Premium History Display

## Status

Ready for Review

## Story

**As a** user of the Insurance Management System UI,
**I want** to see a table of past premium calculations below the calculator form,
**so that** I can review all premiums computed during my session without leaving the page.

## Acceptance Criteria

1. A "Premium History" card section is added below the existing calculator form inside the Insurance Calculator tab in `insurance-client.html`.
2. The history table has four columns: **Timestamp**, **Customer ID**, **Car ID**, **Monthly Premium (R$)**.
3. After a successful premium calculation response, the frontend automatically fetches `GET /insurance-history` and re-renders the history table.
4. A "Refresh History" button is present in the history card and triggers the same fetch-and-render on click.
5. When the history list is empty, the table area displays the message "No premium history yet."
6. The new card and table visually match the existing UI style (same fonts, color palette, border-radius, and box-shadow).
7. No new JavaScript libraries are introduced; implementation uses only `fetch` and vanilla DOM manipulation.
8. The history section is visible only when the Insurance Calculator tab is active (inherits tab visibility naturally — no extra logic needed since it lives inside `<div id="insurance">`).

## Tasks / Subtasks

- [x] Task 1: Add "Premium History" card HTML inside the Insurance Calculator tab (AC: 1, 2, 4, 5, 6, 8)
  - [x] Open `insurance-client.html`
  - [x] Locate `<div id="insurance" class="tab-content">` — find the closing `</div>` of this tab
  - [x] Immediately before the tab's closing `</div>`, add a new card `<div>` with:
    - A heading "Premium History"
    - A "Refresh History" `<button class="btn">` that calls `loadPremiumHistory()`
    - A `<div id="historyTableContainer">` that will hold either the table or the empty-state message
  - [x] Style the card inline using the existing pattern: `background: white; border: 1px solid #e9ecef; border-radius: 8px; padding: 20px; box-shadow: 0 5px 15px rgba(0,0,0,0.1); margin-top: 30px;`
  - [x] The section lives inside `<div id="insurance">` so it is automatically hidden/shown with the tab — no extra visibility logic needed (AC: 8)

- [x] Task 2: Implement `loadPremiumHistory()` JavaScript function (AC: 3, 4, 5, 7)
  - [x] Add `async function loadPremiumHistory()` in the existing `<script>` block
  - [x] Fetch `GET ${API_BASE_URL}/insurance-history` using `fetch()`
  - [x] On success, parse JSON response
  - [x] If array is empty, set `document.getElementById('historyTableContainer').innerHTML` to `<p>No premium history yet.</p>`
  - [x] If array has entries, build an HTML `<table>` with columns: Timestamp | Customer ID | Car ID | Monthly Premium (R$)
    - [x] Use existing table/card styles: no extra CSS class needed beyond inline or matching existing `.result` styling
    - [x] Format `monthlyPremium` to 2 decimal places using `toFixed(2)`
  - [x] On fetch error (backend not running), display empty state message "No premium history yet." — do NOT throw or show a JS error in console (graceful degradation, AC per IV3)
  - [x] Use only `fetch` and vanilla DOM manipulation — no jQuery, no libraries (AC: 7)

- [x] Task 3: Auto-trigger history load after successful calculation (AC: 3)
  - [x] Locate the existing insurance form submit handler in the `<script>` block
  - [x] After a successful `POST /calculate-insurance` response is displayed, call `loadPremiumHistory()`
  - [x] Do NOT call `loadPremiumHistory()` on error responses — only on confirmed successful calculation

- [x] Task 4: Verify integration constraints
  - [x] Manually open `insurance-client.html` in browser (backend running on `localhost:8088`)
  - [x] Confirm all existing tabs (Customers, Cars, Registered Customers, Covered Vehicles, Insurance Calculator) work as before (IV1)
  - [x] Confirm calculator form submit and result display work exactly as before (IV2)
  - [x] Stop backend and reload page — confirm no JS console errors appear and history area shows empty state (IV3)
  - [x] Perform a calculation — confirm history table auto-populates
  - [x] Click "Refresh History" — confirm table re-fetches and renders

## Dev Notes

### Existing Frontend Architecture
[Source: docs/architecture/3-tech-stack.md#31-existing-technology-stack]

- Single-page app: `insurance-client.html` — no framework, pure HTML/CSS/vanilla JS
- `API_BASE_URL = 'http://localhost:8088'` is already defined in the `<script>` block — use this constant for the history fetch URL
- Tab visibility is controlled via CSS class `.tab-content.active { display: block; }` and `.tab-content { display: none; }`
- The Insurance Calculator tab is `<div id="insurance" class="tab-content">` — everything inside it is naturally hidden when another tab is active

### Existing UI Style Reference
[Source: `insurance-client.html` inline styles]

Match the following existing styles for the new card:
```css
/* Card container (same as .car-card / .customer-card pattern) */
background: white;
border: 1px solid #e9ecef;
border-radius: 8px;
padding: 20px;
box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
margin-top: 30px;

/* Button — reuse existing class */
<button class="btn" onclick="loadPremiumHistory()">Refresh History</button>

/* Primary color: #667eea (used for headings, borders, accents) */
/* Success/premium color: #28a745 (used for prices in car cards) */
/* Muted text: #6c757d */
/* Font: inherits from body (system sans-serif stack) */
```

### History Table Structure
Render a standard HTML table. Suggested structure:
```html
<table style="width:100%; border-collapse:collapse; margin-top:15px;">
  <thead>
    <tr style="background:#f8f9fa; color:#333;">
      <th style="padding:10px; border:1px solid #e9ecef; text-align:left;">Timestamp</th>
      <th style="padding:10px; border:1px solid #e9ecef; text-align:left;">Customer ID</th>
      <th style="padding:10px; border:1px solid #e9ecef; text-align:left;">Car ID</th>
      <th style="padding:10px; border:1px solid #e9ecef; text-align:left;">Monthly Premium (R$)</th>
    </tr>
  </thead>
  <tbody>
    <!-- rows generated by JS -->
  </tbody>
</table>
```

### API Contract
[Source: docs/architecture/6-api-design-and-integration.md#get-insurance-history]

- Endpoint: `GET /insurance-history`
- Always returns HTTP 200 — even when empty (`[]`)
- Response fields per entry: `timestamp` (string, ISO-8601 e.g. `"2026-05-26T14:32:01"`), `customerId` (number), `carId` (number), `monthlyPremium` (number)
- Array is newest-first (no client-side sorting needed)

### Fetch Error Handling Pattern
[Source: AC IV3 — graceful empty state on fetch failure]

The fetch should silently fall back to the empty state if the backend is unavailable:
```javascript
async function loadPremiumHistory() {
    const container = document.getElementById('historyTableContainer');
    try {
        const response = await fetch(`${API_BASE_URL}/insurance-history`);
        const data = await response.json();
        if (data.length === 0) {
            container.innerHTML = '<p style="color:#6c757d;">No premium history yet.</p>';
        } else {
            // build and insert table HTML
        }
    } catch (e) {
        container.innerHTML = '<p style="color:#6c757d;">No premium history yet.</p>';
    }
}
```

### File Locations
[Source: docs/architecture/7-source-tree.md#72-new-file-organization]

| File | Type |
|---|---|
| `insurance-client.html` | MODIFIED — add history card HTML and `loadPremiumHistory()` JS function |

No new files are created for this story. No `pom.xml` changes. No new JS libraries.

### Testing Standards
[Source: docs/architecture/10-testing-strategy.md#regression-testing]

- Manual smoke testing required: open `insurance-client.html` in browser, exercise all tabs and the history feature
- No automated frontend tests exist in the project — manual verification is the testing approach for this story
- Automated backend regression: `mvn test` must still pass (no backend changes in this story, but verify Story 1.1 is done first)

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-05-26 | 1.0 | Initial draft | Bob (SM) |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 4.6

### Debug Log References

N/A

### Completion Notes

- All 4 tasks implemented
- History card added inside `<div id="insurance">` — inherits tab visibility automatically
- `loadPremiumHistory()` uses only vanilla `fetch` + DOM manipulation; graceful fallback to empty state on error
- Auto-triggered only on confirmed successful calculation result (inside `if (result.success)` + valid object check)
- Backend regression: all 13 tests pass

### File List

| File | Change |
|---|---|
| `insurance-client.html` | MODIFIED — history card HTML + `loadPremiumHistory()` JS + auto-trigger on success |
